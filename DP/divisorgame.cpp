class Solution {public:    bool divisorGame(int N) {                int dp[N+1];        //dp[i] == 0 if alice loses and 1 if alice wins         memset(dp,0,sizeof(dp));        dp[0]=0;        dp[1]=0;        for(int i=2;i<=N;i++)        {            for(int j=1;j<i;j++)            {                if(i%j==0)                {                    dp[i]=!dp[i-j];                    break;                }            }                    }        return bool(dp[N]);    }};